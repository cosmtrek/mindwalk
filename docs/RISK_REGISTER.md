# RISK REGISTER — Mindwalk Observatory

| ID | Risk | Severity | Status | Mitigation / needed decision |
|---|---|---|---|---|
| R-001 | **Go toolchain not installed** (go.mod needs Go 1.25). Blocked `go test`, `go build`, `gofmt`, and every Go proof gate. | HIGH | **CLOSED 2026-07-13** | Eric approved; Go 1.25.12 installed user-scoped to `~/.local/go` from go.dev, SHA-256 verified before extraction, no sudo/apt. Full baseline now green — evidence: `docs/evidence/baseline-go-2026-07-13.txt`, `docs/BASELINE.md`. |
| R-002 | **Design-language conflict**: upstream `.impeccable.md` explicitly rejects neon cyan/violet + cyberpunk; master prompt Part IV specifies neon blue plasma + cyan/electric violet. | MEDIUM | **CLOSED 2026-07-13** | Eric decided a deliberate hybrid — see `docs/DESIGN_DECISIONS.md` D-001. Owner aesthetic spec overrides `.impeccable.md` aesthetics; its usability/accessibility/restraint guidance stays in force. |
| R-003 | Upstream is young and moving (v0.1.0, ~31+8 commits). Facts drift. | MEDIUM | MITIGATED | Pinned to `97a543c…`; deltas recorded in `docs/BASELINE.md`; re-verify before any upstream sync. |
| R-004 | Master-prompt §II.2 contained stale facts (e.g. said `internal/model` unconfirmed; it exists). | LOW | CLOSED | Reality-divergence rule applied; deltas in `docs/BASELINE.md`. |
| R-005 | Upstream has **no CI test workflow** (release.yml only). Regressions can land silently. | MEDIUM | OPEN | Add a local-first test habit now; consider a free GitHub Actions test workflow in a later checkpoint (needs owner approval to push). |
| R-006 | esbuild postinstall blocked by npm allow-scripts policy. Build works today (vite 7 path), but an esbuild/vite upgrade could start failing. | LOW | OPEN | If a future `make setup` breaks, `npm approve-scripts esbuild` is the fix — flag to Eric first (install-script execution). |
| R-007 | Scope vs. Claude Pro context budget: the master prompt is very large; long chats lose state. | MEDIUM | MITIGATED | Small checkpoints; durable state in `docs/PROGRESS.md`; targeted file reads only (§I.6). |
| R-008 | **No `origin` remote** — all local branches exist only on this laptop. Disk failure loses work. | MEDIUM | OPEN — ACKNOWLEDGED (Eric, 2026-07-13) | Deliberately deferred: Eric will create his GitHub repository after the baseline is green (done) **and** the Phase 0 documentation diff is reviewed. No origin is created or pushed until then. |
| R-009 | Kali rolling upgrades can shift Node/npm/Go underneath the project. | LOW | OPEN | Toolchain versions recorded per baseline; re-record on drift; consider pinning Node via `engines` in a later checkpoint. |
| R-010 | Playwright's bundled Chromium is absent. | LOW | MITIGATED 2026-07-13 | `/usr/bin/chromium` is installed and a real Playwright onboarding flow passed with `executablePath`; no browser download is needed. |
| R-011 | Live ingestion primitives existed without source-to-ledger proof; documentation could overstate P3. | HIGH | **CLOSED 2026-07-13** | Source-to-ledger/projector/durable-SSE wiring and synthetic Claude/Codex restart browser replay pass; LIVE-001..003 and SEC-001 are TESTED with `.local-evidence/p3-source-browser-2026-07-13.txt`. |
| R-012 | Empty comparison/search slices could serialize as `null` and crash array-oriented UI code. | MEDIUM | **CLOSED 2026-07-13** | Read models now return non-null arrays; API regression tests and the full Review/Memory browser flow pass. |
| R-013 | Backup restore could accept link/special archive entries or leave a partial two-directory apply. | HIGH | **CLOSED 2026-07-13** | Restore preflights type/path/size/count, rejects symlink targets, stages both trees, and rolls back errors; malicious-link smoke passes. |
| R-014 | New SQLite/npm dependency vulnerabilities or incomplete component inventory. | MEDIUM | **MITIGATED 2026-07-13** | `govulncheck` and production `npm audit` report none; combined CycloneDX 1.5 SBOM validates 163 unique refs. Re-run on dependency changes. |
