# OPEN SOURCE INVENTORY

As of upstream pin `97a543c` (2026-07-13). Source of truth for licenses:
`THIRD_PARTY_NOTICES.md` (read from installed packages, not from memory).

## Go

- Direct dependency: `modernc.org/sqlite v1.53.0` (BSD-3-Clause), a
  CGO-free SQLite driver used only for the local, rebuildable FTS5 memory
  index.
- 24 transitive module versions are pinned by `go.sum`. Their installed
  license files are permissive BSD/MIT/Go-style licenses; no copyleft module
  was identified. Exact module/version refs are in `docs/sbom.cdx.json`.

## npm (web/)

- 17 direct dependencies (9 runtime, 8 dev) — full table with versions and
  licenses in `THIRD_PARTY_NOTICES.md`.
- 138 unique package/version components are pinned by
  `web/package-lock.json`; `npm audit --omit=dev` reported **0
  vulnerabilities** on 2026-07-13.
- License classes present: MIT, ISC, Apache-2.0, OFL-1.1 (fonts). No copyleft.

## Security and machine-readable inventory

- `docs/sbom.cdx.json`: CycloneDX 1.5, 163 unique Go/npm component refs,
  generated by `node scripts/generate-sbom.mjs`.
- `govulncheck ./...`: no reachable Go vulnerabilities found on 2026-07-13.
- `npm audit --omit=dev`: no production npm vulnerabilities found on
  2026-07-13.

Update this file whenever a dependency is added, removed, or upgraded, in the
same checkpoint as the change.
